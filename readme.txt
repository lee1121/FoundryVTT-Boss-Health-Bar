A module to add a Dark Souls-like health bar for bosses in FoundryVTT.

Currently works by using macro's.

To toggle the visibility of the health bar:
```
BossHealthBar.toggleVisibility()
```

To toggle which token the health bar is using:
```
BossHealthBar.selectSelectedToken()
```